<!DOCTYPE html>
<html>
    <head>
        <title>Kalkulator</title>
        <link type="text/css" rel="stylesheet" href="/css/bootstrap.css">
        <link type="text/css" rel="stylesheet" href="/css/landing.css">
    </head>
    <body>
        <nav class="navbar navbar-default">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header <% if (typeof beradaLanding === "string") { %> <%= beradaLanding %> <% } %>">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    </button>
                <a class="navbar-brand" href="/">kalkulator</a>
                </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li <% if (typeof beradaHome === "string") { %> class="<%= beradaHome %>" <% } %> >
                        <a href="/calculators">
                        <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
                        Home</a>
                    </li>
                    <li <% if (typeof beradaAbout === "string") { %> class="<%= beradaAbout %>" <% } %>>
                        <a href="/about"><span class="glyphicon glyphicon-info-sign" aria-hidden="true"></span>
                        About</a>
                    </li>
                    <li <% if (typeof beradaContact === "string") { %> class="<%= beradaContact %>" <% } %>>
                        <a href="/contact"><span class="glyphicon glyphicon-link" aria-hidden="true"></span>
                        Contact</a>
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                        <% if (!currentUser) { %>
                            <li <% if (typeof beradaLogin === "string") { %> class="<%= beradaLogin %>" <% } %> ><a href="/login"><span class="glyphicon glyphicon-flash" aria-hidden="true"></span>
                            Login</a></li>
                            <li <% if (typeof beradaRegister === "string") { %> class="<%= beradaRegister %>" <% } %> ><a href="/register"><span class="glyphicon glyphicon-new-window" aria-hidden="true"></span>
                            Sign Up</a></li>
                        <% } else { %> berada
                            <li <% if (typeof beradaUser === "string") { %> class="<%= beradaUser %>" <% } %> >
                                <a href="/user">
                                <span class="glyphicon glyphicon-user" aria-hidden="true"></span>
                                Signed In As <%= currentUser.username %>
                                </a>
                            </li>
                            <li>
                                <a href="/logout"><span class="glyphicon glyphicon-off" aria-hidden="true"></span>
                                Logout</a>
                            </li>
                        <% } %>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
        </nav>

        <div class="container">
            
            <% if(error && error.length > 0){ %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if(success && success.length > 0){ %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>